/* task: Завершите функцию, чтобы она возвращала имя в верхнем регистре. */

function getUpperCaseName(name){
	return name.toUpperCase();
}

getUpperCaseName("sam"); // SAM
getUpperCaseName("alex"); // ALEX
getUpperCaseName("Johnny"); // JOHNNY